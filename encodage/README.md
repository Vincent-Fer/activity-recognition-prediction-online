Contient les deux fichiers d'encodage d'image :
1. main.py : encodage de base (normalisation une fois que l'on a toutes les positions)
2. encodage.py : encodage unitaire (normalisation avec la position précédente, en blanc si la jointure ne bouge pas)
